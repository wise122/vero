export default async ({redirect, store, app, route}) => {
    console.log("masuk auth landing")
    // if(true) {
        // if(!['maintenance','quickregister'].includes(route.name)) redirect({name:'maintenance'})
    // } else {
        // const checkToken = app.$cookies.get('token') ? true : false
        // if (checkToken) {
        //     redirect({name: 'index'})
        // }
    // }
}